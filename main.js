function verifyAge(){
    let age = document.getElementById("age").value;
    if (age < 18) {window.alert("Sorry, you're too young to fill out the form. But not for sean's type.");} else{window.alert("You can now vote.")}
}
    
   